apiVersion: v1
kind: Service
metadata:
  name: image-render-server
spec:
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: image-render-server
