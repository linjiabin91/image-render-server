apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

# 具体标签值由各 overlay 覆盖，避免多引擎部署时 service/pod 串扰
labels:
  - pairs:
      app: placeholder
    includeSelectors: true
