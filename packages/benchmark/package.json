{
  "name": "@image-render-server/benchmark",
  "description": "HTTP 压测工具 — 基于 autocannon 的渲染服务性能测试",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "benchmark": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "benchmark": "node dist/index.js",
    "benchmark:help": "pnpm benchmark --help"
  },
  "dependencies": {
    "autocannon": "^8.0.0"
  },
  "devDependencies": {
    "@types/autocannon": "^7.0.0",
    "@types/node": "^25.9.2"
  }
}