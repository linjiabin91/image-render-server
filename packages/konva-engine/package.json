{
  "name": "@image-render-server/konva-engine",
  "description": "Konva 渲染引擎 — 基于 konva + @napi-rs/canvas 的图片渲染实现",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -b",
    "start": "pnpm run build && LOG_PRETTY=true node dist/server.js"
  },
  "dependencies": {
    "@napi-rs/image": "^1.12.0",
    "@image-render-server/core": "*",
    "@image-render-server/server": "*",
    "konva": "^10.3.0",
    "skia-canvas": "^3.0.8"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
